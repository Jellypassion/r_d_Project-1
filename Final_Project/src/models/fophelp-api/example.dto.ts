// Example DTO for a Fophelp API entity
// Replace this with actual DTOs based on the API documentation

export interface ExampleDto {
    id: string;
    name: string;
    createdAt: string;
    updatedAt: string;
}

export interface CreateExampleDto {
    name: string;
}

export interface UpdateExampleDto {
    name?: string;
}
